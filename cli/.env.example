# Never commit a populated copy. Prefer a dedicated, limited-funds wallet.
# Required only for writes and wallet-balance lookups.
PRIVATE_KEY=

# Optional: prepend a private RPC to the built-in fallback list.
# Replace BASE with any uppercase CLI key; use underscores for separators.
# MINTCLUB_RPC_BASE=https://your-rpc.example
# MINTCLUB_RPC_BASE_SEPOLIA=https://your-rpc.example
